# Integrations

Server-only adapters for external providers belong here. Provider credentials
must be read through `src/server/config` and must never be imported by browser
modules. Scientific, AI, payment and notification integrations are intentionally
not implemented in this phase.
